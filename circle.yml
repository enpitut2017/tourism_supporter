dependencies:
  pre:
    - wget http://owl.phy.queensu.ca/~phil/exiftool/Image-ExifTool-10.60.tar.gz
    - tar -zxvf Image-ExifTool-10.60.tar.gz
    - cd Image-ExifTool-10.60 && perl Makefile.PL && make test && sudo make install
